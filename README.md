# Cardano Savings Vault (Vercel Ready)

## 🚀 How to Deploy in 30 Seconds:
1. Create a NEW GitHub repository.
2. **UPLOAD ALL FILES** inside this zip directly to the root of your repo.
3. Import that repo into **Vercel**.
4. Vercel will auto-detect everything. **Do not change any settings.**

### Key Fixes Included:
- **Project Structure**: Files are at the ROOT level (no nested folders).
- **WASM Support**: Configured in `next.config.js` for Mesh SDK.
- **Next.js 14**: Optimized for the latest Vercel build environment.
